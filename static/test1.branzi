{
    a : int := 77;
    b : int := 23 + a;

    l : list int := [1, (3+4)*2, a, b];

    lempty : list bool := [];
    lsingle : list (list int) := [l, l];

    llol : list any := [lempty];
    llol2 : list (list bool) := [lempty];

    lol3 : int := [];

    if (true and (false or true)) {
        a : int := 2;
        c : int := a + b;

        l[0] = c;

        d : int := l[1] - c;
    }
}